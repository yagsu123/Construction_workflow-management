node_modules/
.next/
out/
# The live database lives in ~/.pwd-infra-workflow (see src/config.js). These are stale files
# from an earlier phase that OneDrive holds a lock on and will not let us delete.
data/
public/uploads/*
!public/uploads/.gitkeep
.env*.local
.DS_Store
.commit-msg.txt
.commit-msg2.txt
